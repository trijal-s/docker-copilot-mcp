# docker-copilot-mcp
A local MCP server for Claude that inspects Docker containers and safely restarts them — with scope whitelisting, dry-run confirmation, and full audit logging for any destructive action.
